# Arbitrary-Rank Ablation（ARA）技術調査レポート

作成日：2026-09-13

対象：ローカル `ara` ブランチ、コミット `edc3b123456c7f86f24d409b838ab3a7226e285e`。

本稿はソースコードの静的調査と公開資料に基づく。モデルの実行、ループの再現実験、性能測定は実施していない。会話の前段で確認した `ara-gemma4` とはコードに差があるため、以下の実装上の指摘は上記スナップショットに限定する。改善案は独立した品質評価・診断・運用上の保護を対象とし、拒否抑制能力を高める損失関数の変更や調整手順は扱わない。

## 1. 要点

ARA は、特定の拒否方向への射影を前提とせず、モジュールの入出力を観測して重み行列を直接変更する手法である。名称の arbitrary-rank は、更新を単一方向の低ランク変換に限定しないことを指す。変更後のモデルの品質や安定性を保証する名称ではない。[原作者の提案](https://github.com/p-e-w/heretic/pull/211)

本実装を評価するうえで中心的な問題は、局所的な内部表現の変化と、長い回答全体の品質が異なる対象だという点にある。現在の評価は、先頭位置の分布差と文字列ベースの拒否判定、または PIQA を用いている。反復、正常終了、内容の進展を直接評価する仕組みは確認できない。[評価実装](src/heretic/evaluator.py)

ユーザーから報告された「拒否の代わりにループする」現象は、調査すべき重要な品質問題である。ただし、現時点では発生頻度も因果関係も未検証であり、ARA 一般の確定した性質としては扱わない。

## 2. 仕組みと評価の二層構造

### 2.1 内部表現を使った変更

`get_module_io()` はフックを使い、各プロンプト末尾位置のモジュール入出力を収集する。`ara_abliterate()` は、通常入力に対する出力の保持と、対象入力に対する出力群の変化を組み合わせた目的関数で、モジュールごとに最適化する。実装には L-BFGS が使われている。[モデル実装](src/heretic/model.py)

この設計では、内部表現の幾何学的な距離が代理指標になる。「出力群が近い」ことから、「正しい回答を最後まで生成できる」ことが直接導かれるわけではない。

### 2.2 Full-weight と LoRA

本スナップショットには、重みを直接変更する経路と LoRA を使う経路がある。後者の更新はアダプターのランクに制約されるため、Full-weight と同じ自由度とは限らない。両者の結果は別条件として評価すべきである。[モデル実装](src/heretic/model.py)

行ノルムの保持も、行列全体の作用を保持する保証ではない。同じ行ノルムでも、方向、行同士の関係、特異値分布は異なりうる。これは線形代数上の性質であり、当該モデルでの実害を実測したものではない。

### 2.3 候補モデルの外部評価

`main.py` は変更後に `Evaluator.get_score()` を呼び出す。内部損失の値と、候補モデルを比較する評価値は別物である。[呼び出し元](src/heretic/main.py)

| 評価項目 | 現在の処理 | 読み取れる範囲 |
|---|---|---|
| 拒否 | マーカー文字列の部分一致 | 明示的な表現の検出 |
| 空回答 | 拒否として集計 | 生成失敗と拒否を区別できない |
| KL | 先頭生成位置の分布を比較 | その位置での基準モデルとの差 |
| PIQA | `acc_norm` を取得 | 単一課題での成績 |
| PIQA と KL | 切り替え式 | 同時に記録されない |
| ループ | 専用判定なし | 発生率を直接把握できない |

なお、`response_prefix` が設定されている場合、KL の評価位置はその接頭辞を付けた後になる。常に会話応答の最初の文字に相当するとは限らない。[生成・確率取得処理](src/heretic/model.py)

## 3. 確認できる欠点と限界

### 3.1 長い生成への一般化を保証しない

観測するモジュール入出力はプロンプト末尾位置に限られる。生成途中の状態、長文での依存関係、終了に至る状態遷移は、それだけでは評価できない。また、各モジュールでの局所的な保持は、それらを組み合わせたモデル全体の挙動保持を保証しない。[観測処理](src/heretic/model.py)

### 3.2 拒否数は回答品質を表さない

拒否マーカーを含まない反復文は、現状の判定では非拒否になる。これはコードから確認できる評価上の盲点である。一方、拒否表現を引用した説明文が拒否として数えられる可能性もある。したがって、非拒否を成功回答と読み替えてはいけない。[拒否判定](src/heretic/evaluator.py)

### 3.3 先頭 KL と単一課題では測定範囲が狭い

先頭位置の KL が小さくても、後続の反復や誤答は起こりうる。逆に、言い換えや文体差で KL が増えても、回答の正しさが下がるとは限らない。PIQA を選んだ場合も、自由生成の終了性、日本語、長文整合性などを同時に測定しているわけではない。

さらに、現在の `get_score()` は KL が閾値未満になると、その目的値を拒否率に依存する式に置き換える。同じ拒否率で閾値未満なら、実測 KL の違いがその目的値に反映されない。[スコア処理](src/heretic/evaluator.py)

### 3.4 数値最適化の保証には注意が必要

原作者の提案には凸性に関する説明があるが、その記述を実装全体の収束保証として受け取るべきではない。[原作者の説明](https://github.com/p-e-w/heretic/pull/211)

コード上は最近傍集合の切り替わり、距離項の減算、条件付きの行正規化があり、LoRA 経路では二つの行列の積を変数として扱う。これらから、一般的な滑らかな凸最適化の保証をそのまま適用する根拠は得られない。これは静的な数学的評価であり、実際に発散したとの報告ではない。[距離計算](src/heretic/utils.py)、[目的関数](src/heretic/model.py)

調査対象の `ara` では、Full-weight の更新ループおよび KL 計算に、以前確認した `ara-gemma4` にあった明示的な非有限値対策は見当たらなかった。数値異常の有無を実行結果から確認する必要がある。

## 4. ループ現象の整理

### 4.1 区別するべき現象

| 現象 | 例 | 判定上の注意 |
|---|---|---|
| 完全反復 | 同じ短文が連続する | 引用、詩、規則的なデータにも反復はある |
| 周期反復 | A、B、A、B と続く | 単一文の連続重複では見逃す |
| 意味的反復 | 言い換えだけを繰り返す | 文字列一致だけでは検出しにくい |
| 説明を続ける非回答 | 背景や一般論を述べ続け、求められた答えを示さない | 語彙や話題が変化しても要求の達成は進まないことがある |
| 終了失敗 | 内容が続き、上限まで止まらない | 長い正常回答と区別する必要がある |
| 表示側の重複 | 同じ断片が画面に追加され続ける | モデルの出力トークン自体は正常な可能性がある |

「上限到達」と「ループ」は同義ではない。また、反復を途中停止しても、元のモデルが正常な回答を作れるようになったとはいえない。

ユーザーからは、明確な拒否をせず、説明や反復を続けながら実際には答えない事例も報告された。本稿ではこれを「説明を続ける非回答」として記録する。これは本稿での記述用の名称であり、ARA 固有の機構や確立した診断名を意味しない。実際の応答例は未確認である。

例えば、通常用途の「二つの文章の相違点を三つ挙げて」という依頼に対し、比較の重要性や比較方法だけを述べ、相違点を一つも挙げない応答が該当しうる。文章として流暢で、同一フレーズの反復がなく、終端トークンで終了していても、要求は満たされていない。

この現象は「内容が進展しない反復」と重なる場合もあれば、一度の説明だけで終わる非回答の場合もある。したがって、すべてを生成ループと同一視せず、反復の有無と要求への回答の有無を分けて記述する。表面的な非回答だけから、潜在的な拒否が残っている、意図的に回避している、または ARA が原因であるとは断定できない。

冗長でも最終的に答えている応答、情報不足に対する必要な確認質問、答えられない理由を明示した応答は区別する必要がある。

### 4.2 原因候補と証拠の強さ

| 仮説 | 根拠 | 未確認事項 |
|---|---|---|
| 重み変更によって後続生成が不安定化 | 局所保持と系列全体の安定性は別 | 同一入力で基準モデルより悪化するか |
| デコード方式が反復を強める | 現実装は greedy decoding | 今回のループで主要因か |
| 終端トークンやテンプレートの不整合 | 終了処理の一般的な診断候補 | 使用モデルの設定と一致しているか |
| 推論基盤や表示処理の問題 | 重複表示と生成反復は別 | 生のトークン列でも反復しているか |

一般に、尤度を最大化するデコードで文章が反復・退化する現象は、ARA 以外でも研究されている。その文献は今回の原因を確定するものではない。[Holtzman ほか『The Curious Case of Neural Text Degeneration』](https://arxiv.org/abs/1904.09751)

## 5. 改善案：独立した品質監査

以下は、一般的な言語モデルの品質検証としての提案である。ARA の拒否抑制探索に組み込む具体的な最適化方法は対象外とする。

### 5.1 回答の状態を分離して記録する

拒否、空回答、反復、上限到達、正常終了、要求への回答の有無、正誤を別項目として記録する。例えば「拒否文を反復して上限到達」は複数項目に該当するため、排他的な一分類に押し込めない。

通常用途の品質監査では、「完全な回答」「部分的な回答」「説明のみで非回答」「必要な確認・情報不足」などを区別し、依頼と応答を一緒に確認する。回答の有無と正しさも別であり、具体的に答えた誤答を成功に数えない。流暢さ、文字数、語彙の多様性、拒否マーカーの不在だけで要求達成を推定しない。

レポートでは件数と母数を併記し、集計値から問題の回答例に戻れるようにする。マーカー一致だけで意味的な拒否や成功を確定せず、抽出例を人手でも確認する。

### 5.2 系列全体と実用課題を併せて評価する

固定した通常用途のデータで、回答途中を含む分布差、課題正答率、指示遵守、終了状態を別々に報告する。複数位置の KL を測る場合は、両モデルに同じ接頭列を与えた比較と、自由生成による品質評価を区別する。異なる生成履歴での分布差には、履歴の差も含まれるためである。

基準モデルからの変化と絶対成績を併記する。基準モデル自体の誤答や反復を保つことは、品質改善ではない。

### 5.3 反復検出器自体を評価する

トークン反復、文の重複、周期性など複数の観測を使い、人が正常・異常を確認したデータで誤検出と見逃しを測る。日本語、コード、表、歌詞以外の詩、定型文など、正常でも繰り返しが現れる形式を含める。

意味的反復を別モデルで判定する場合も、その判定は正解ラベルではない。人手との一致、不一致例、判定モデルのバージョンを残す。

### 5.4 実験条件とデータを固定する

開発用データと最終評価用データを分離し、最終評価の結果を見て繰り返し調整しない。同一の通常用途入力について基準モデルと比較し、モデル revision、コード commit、テンプレート、トークナイザー、精度・量子化、生成上限、乱数設定、実行環境を記録する。

greedy decoding でも、ハードウェアや数値計算の違いをまたいだ完全一致が保証されるわけではない。条件が異なる結果を、重み変更だけの効果として解釈しない。

### 5.5 推論時の保護と品質改善を別に扱う

一般的な運用対策として、生成量・時間の上限、利用者によるキャンセル、反復検知による停止を設ける。停止理由を保存し、正常終了として数えない。

保護機能を付ける前の異常発生率と、付けた後に抑えられた無駄な生成量を別々に測る。これにより「ループを止めた」と「回答品質が回復した」を混同せずに済む。

## 6. 推奨する調査順序

1. 既存のループ事例について、生の生成列・終了理由・実行設定を保存し、表示上の重複と区別する。
2. 通常用途の同一入力を基準モデルでも実行し、変更後だけで起きる現象かを確認する。
3. 空回答、反復、説明のみの非回答、上限到達、正誤を独立して集計し、人手確認した例を添える。
4. 日本語・長文・構造化出力を含む固定の品質評価を行う。
5. 推論時の停止処理の効果を、モデル自体の品質変化とは別に測る。

現段階で確実にいえるのは、現在の評価ではループを品質不良として直接測れていないことまでである。ARA が原因か、どの程度の頻度で起きるか、どの対策が有効かを判断するには、再現可能な出力例と対照実験が必要になる。

## 7. 詳細設計案：通常用途の独立品質監査

本節は新規の設計提案であり、実装済みの機能や、実験で有効性を確認した手法ではない。通常用途の回答品質を測定するオフライン監査を想定する。ARA の損失関数、拒否抑制の報酬、探索パラメーターへの接続は含めない。

### 7.1 評価対象を先に定義する

評価単位は「入力・必要な文脈・応答・実行条件」の組とする。応答だけでは、要求達成や必要な確認質問を判定できない。

回答を見る前に、各課題に次の情報を付ける。

| 項目 | 内容 |
|---|---|
| 要求事項 | 回答に必要な成果物・結論・形式 |
| 判定根拠 | 正解、検証可能な資料、または人手の採点基準 |
| 回答可能性 | 与えられた情報で回答可能、確認が必要、判定不能 |
| 許容される応答 | 直接回答、条件付き回答、必要な確認など |
| 誤検出要因 | 正常な反復、定型構造、長い前提説明の必要性 |

課題の要求を応答に合わせて事後的に変えない。自動生成した採点基準も、採点前に人が点検する。情報不足や適切な拒否を、説明のみの非回答と一律に扱わない。

### 7.2 単一得点ではなく測定ベクトルを保存する

監査結果は、要求充足、正確性、進展、反復、終了状態、判断の確かさを別々に保存する。各値の定義と母数を明記し、未判定をゼロ点に変換しない。

要求事項が m 個ある場合、診断用の要求充足率を次のように定義できる。

`coverage = sum(w_i * a_i) / sum(w_i)`

ここで `a_i` は要求事項 i に対応する回答が存在すれば 1、存在しなければ 0、判断できなければ未判定とする。重み `w_i` は回答を見る前に固定する。部分的な充足は可能なら要求事項を分割して表現し、採点者ごとの曖昧な小数点採点を避ける。必須事項の欠落は平均値に埋めず、個別に表示する。

この値は「答えを提示した割合」であり、「正しい答えの割合」ではない。例えば、相違点を三つ挙げたが三つとも誤っている応答は、存在としての充足と正確性で異なる結果になる。判定不能な事項があれば、その個数と、確認できた事項だけを母数とすることを表示する。要求事項が空なら計算しない。

形式適合も別項目にする。正しい JSON であること、三項目あること、内容が正しいことは、それぞれ独立した性質である。

### 7.3 非回答と「進展のない説明」を区別する

最終的な非回答判定と、途中の進展の診断は別に行う。

- **最終判定**：回答可能な要求について、必要な内容が最終応答に存在するかを確認する。
- **過程の診断**：段落などの意味単位ごとに、新しく満たされた要求事項と、その根拠となる箇所を記録する。
- **停滞候補**：説明が追加されても要求充足が進まず、新しい根拠・必要な前提も増えていない区間として、人手確認に回す。

単に要求充足率が増えない区間をループとしない。数学の導出、背景を先に示す解説、最後に結論を置く文章では、正常でも中間段階の充足率が変わらないことがある。

途中に答えらしき記述が出た場合も、後で撤回・矛盾していれば最終回答として数えない。「一度でも記載した事項」の履歴と、「最終的に支持されている回答」の状態を分ける。

この診断は、単語の新規性や文章間の類似度だけでは代替できない。同じ内容の言い換えは語彙が新しくても停滞し、同じ用語を繰り返す導出は内容が進むことがある。類似度は確認対象を選ぶ補助情報として扱う。

### 7.4 評価パイプラインの構成

```text
固定した課題仕様 + 保存済み生成記録
              |
              v
入力検証・欠損確認
              |
              v
機械的検査（空回答、形式、反復、終了状態）
              |
              v
意味評価（要求との対応、根拠箇所、非回答、正確性）
              |
              v
人手監査（判定不能、不一致、正常例の無作為抽出）
              |
              v
項目別集計・基準モデルとの差・監査記録
```

生成と採点を別工程にして、採点器を更新しても同じ生成記録を再評価できるようにする。通常は文章の表示用正規化と、反復検出用正規化を分離し、元の文章を保持する。

以下は記録形式の案である。フィールドは実装上の既存仕様ではない。

| フィールド | 保存する内容 |
|---|---|
| `case_id` / `run_id` | 課題と生成実行の識別子 |
| `task_spec_version` | 採点基準の版 |
| `model_revision` / `generation_config` | 再現に必要なモデル・生成条件 |
| `raw_response` / `token_ids` | 加工前の応答と取得可能なトークン列 |
| `stop_reason` | EOS、上限、キャンセル、検知停止、不明 |
| `requirement_results` | 要求ごとの有無、正確性、証拠箇所、未判定理由 |
| `quality_flags` | 空回答、反復、説明のみの非回答などの複数ラベル |
| `evaluator_version` | 規則・判定モデル・採点プロンプトの版 |
| `review_status` | 自動判定、人手確認済み、不一致保留 |

取得できなかった終了理由を文章から推測して EOS と記録しない。判定用モデルに渡した応答は評価対象データであり、その中の指示を採点器への指示として扱わない。

### 7.5 意味評価器の信頼性を測る

評価器の検証用データと、評価器の規則・閾値を調整するデータを分離する。評価器の入力には必要な課題仕様を渡し、比較対象のモデル名は可能な範囲で隠す。

少なくとも、直接回答、部分回答、正しい長い説明、誤答、説明のみの非回答、必要な確認、正常な反復を含める。人手ラベルに不一致がある場合は、原因が課題の曖昧さか採点基準の曖昧さかを確認する。

検出性能は全体の正解率だけでなく、非回答・反復などのラベル別に、適合率、再現率、誤検出例を報告する。問題例ばかりを抽出して検証すると、実際の利用時の適合率を推定できないため、通常例の無作為抽出も含める。

判定モデルの自己申告の確信度を、そのまま正解確率とは扱わない。実際の人手一致率と照合し、判断を保留した割合と、判定した範囲での誤りを併記する。根拠箇所が提示されても、その根拠とラベルが整合しているかを確認する。

### 7.6 比較実験と集計

基準モデルと対象モデルには同じ課題を用い、課題ごとの差を対応付けて集計する。同一課題から複数の生成を得た場合、それらを独立した別課題として母数を水増ししない。

言語、文章長、課題形式ごとの結果を併記する。全体平均だけでは、日本語や構造化出力など一部の悪化を見逃す可能性がある。信頼区間を付ける場合も、再標本化などの単位は基本的に課題とし、同じ課題内の生成の関係を維持する。

少数例で「観測されなかった」ことを「起こらない」と解釈しない。モデル差を議論する際は、件数、母数、未判定数、条件差を示す。モデル変更とデコード設定変更を同時に行った比較から、モデル変更だけの効果を断定しない。

### 7.7 運用上の保護を別の実験として検証する

反復停止などの保護については、異常な生成を停止できた割合に加えて、正常な回答を途中で止めた割合も測る。実行時間と生成トークン数の削減を報告する場合は、要求達成率の変化も併記する。

保護を有効にすると、停止後にモデルがどう続けたかは観測できなくなる。この打ち切りを無視して、ループの自然発生率が下がったと主張しない。保存済みの完了記録を使った検出器の評価と、オンライン停止の評価は区別する。

### 7.8 実装を始める際の段階的な成果物

| 段階 | 成果物 | 確認事項 |
|---|---|---|
| 仕様化 | 通常用途の課題仕様と人手採点例 | 正常な説明と非回答を区別できるか |
| 記録 | 再採点できる生成記録 | 終了理由・条件・欠損が追跡できるか |
| 機械的検査 | 形式・空回答・反復の検査結果 | 正常な反復を誤検出していないか |
| 意味評価 | 根拠付きの要求対応表 | 判定器の誤りと未判定が可視化されるか |
| 比較報告 | 項目別・課題別の比較 | 正確性低下や部分集団の悪化が隠れていないか |
| 運用評価 | 停止機能の効果と副作用 | 停止を品質回復と誤認していないか |

まず評価器の妥当性を確かめ、その後にモデル間の差を解釈する。この順序により、「拒否表現がない」「反復が短くなった」「採点器の点数が高い」といった観測を、実際の要求達成と取り違えることを減らせる。

## 8. 数式による定義：品質監査器の検証

本節の関数は、通常用途のデータにおける監査器の信頼性と比較結果を測るための設計案である。ARA の更新・探索に与える損失や報酬ではない。単一の合成点にまとめず、測定対象と分母を明示した関数として扱う。

### 8.1 データと記号

人手で確認した監査データを次のように表す。

$$
\mathcal{D}=\{(x_i,y_i,z_i,\hat z_i)\}_{i=1}^{N}
$$

ここで、`x_i` は課題と必要な文脈、`y_i` は応答、`z_i` は人手ラベル、`hat z_i` は監査器の出力である。ラベルは、例えば通常用途での反復の有無など、一つの性質ごとに定義する。複数の性質を一つの正解ラベルに混ぜない。

以下では `z_i ∈ {0,1}` とし、1 を検出対象の品質不良とする。監査器は判定保留 `⊥` を返せるものとする。人手でも判断できない例はこの二値集計の外に置き、その件数を別に報告する。

### 8.2 誤検出と見逃しを測る関数

判定した例の集合を `S = {i : hat z_i ≠ ⊥}` とする。各件数を次で定義する。

$$
\begin{aligned}
TP &= \sum_{i\in S}\mathbf{1}[z_i=1\land\hat z_i=1],\\
FP &= \sum_{i\in S}\mathbf{1}[z_i=0\land\hat z_i=1],\\
TN &= \sum_{i\in S}\mathbf{1}[z_i=0\land\hat z_i=0],\\
FN &= \sum_{i\in S}\mathbf{1}[z_i=1\land\hat z_i=0].
\end{aligned}
$$

$$
\operatorname{Precision}=\frac{TP}{TP+FP},\qquad
\operatorname{Recall}_{S}=\frac{TP}{TP+FN},\qquad
\operatorname{FPR}_{S}=\frac{FP}{FP+TN}.
$$

添字 S は「判定した範囲での値」であることを示す。分母がゼロなら値は未定義として保存し、便宜的に 0 や 1 に置き換えない。

判定保留に難しい例を集めれば、これらの数値だけは改善できる。したがって、次の判定率も必ず併記する。

$$
\operatorname{DecisionCoverage}=\frac{|S|}{N},\qquad
\operatorname{PositiveCoverage}=
\frac{\sum_{i\in S}\mathbf{1}[z_i=1]}
{\sum_{i=1}^{N}\mathbf{1}[z_i=1]}.
$$

この coverage は「監査器が判定した割合」であり、第7章の要求充足率とは異なる。全人手陽性例のうち自動的に検出できた割合も別に記録する。

$$
\operatorname{DetectionYield}=
\frac{TP}{\sum_{i=1}^{N}\mathbf{1}[z_i=1]}.
$$

判定保留を陰性と偽って記録せず、未解決分を含めた実際の検出範囲を示せる。

### 8.3 確率出力の検証

監査器が品質不良の確率として `p_i ∈ [0,1]` を出力する場合、固定した検証データ上で Brier score を計算できる。

$$
\operatorname{BS}=\frac{1}{N}\sum_{i=1}^{N}(p_i-z_i)^2.
$$

これは確率予測の誤差を測るもので、応答自体の品質点ではない。また、較正だけを純粋に測る指標でもない。品質不良の少ないデータでは、常に低確率を返すだけでも小さくなりうるため、クラス比率と定数予測の結果を併記する。

確率帯ごとの一致も確認する。あらかじめ定めた確率帯に属する例の集合を `B_b` とすると、

$$
\bar p_b=\frac{1}{|B_b|}\sum_{i\in B_b}p_i,\qquad
\bar z_b=\frac{1}{|B_b|}\sum_{i\in B_b}z_i.
$$

各帯で平均予測確率と実際の陽性率を、件数とともに示す。空の帯は計算しない。モデルの自己申告値が高いことと、実際に正しいことは別である。

### 8.4 判定保留を含む誤りの表示

人手データで意味を検証した確かさの値を `q_i`、判定を採用する境界を `τ` とし、`S_τ = {i : q_i ≥ τ}` とする。

$$
C(\tau)=\frac{|S_\tau|}{N},\qquad
R(\tau)=\frac{1}{|S_\tau|}
\sum_{i\in S_\tau}\mathbf{1}[\hat z_i\ne z_i].
$$

`C` は判定率、`R` は判定した範囲での誤り率である。二つを組として報告する。`S_τ` が空なら `R` は未定義となる。この表示は監査器の検証用であり、生成モデルの目的関数ではない。

### 8.5 基準モデルとの対応付き比較

課題 i における通常用途の監査項目の測定値を、対象モデルについて `m_i^A`、基準モデルについて `m_i^B` とする。値の方向は項目ごとに明示する。例えば反復発生なら大きいほど悪い。

$$
d_i=m_i^A-m_i^B,\qquad
\widehat{\Delta}=\frac{1}{N}\sum_{i=1}^{N}d_i.
$$

同一課題を対応付けることで、課題構成の差を混ぜずに比較する。同じ課題から複数生成した場合は課題内の平均を先に求め、課題数を母数とする。片方が欠損した課題は対応付き比較に使えないため、除外数と理由を示す。

この差は因果効果の証明ではない。生成設定、量子化、テンプレートなどに差があれば、その差も結果に含まれる。区間推定には課題単位の再標本化などを用い、同じ課題の生成を独立標本として扱わない。

### 8.6 停止機能の評価関数

人手確認した完了済み記録に対するオフライン検査では、品質不良の有無を `z_i`、停止検出器が発火したかを `a_i ∈ {0,1}` として、次の二つを分けて計算する。

$$
\operatorname{FalseStopRate}=
\frac{\sum_i\mathbf{1}[z_i=0\land a_i=1]}
{\sum_i\mathbf{1}[z_i=0]},\qquad
\operatorname{DetectionRate}=
\frac{\sum_i\mathbf{1}[z_i=1\land a_i=1]}
{\sum_i\mathbf{1}[z_i=1]}.
$$

前者は正常回答を止めてしまう割合、後者は品質不良を検出できた割合である。判定には必要な文脈を含め、正常な反復を品質不良にしない。

オンラインで停止した記録では、その後に自然終了したかどうかは観測できない。このため、停止済み応答だけを使って上式の正解ラベルを作ると循環した評価になる。完了済み記録による検証と、実運用での停止件数・コストの報告を分ける。

### 8.7 関数としてのインターフェース案

次は実行コードではなく、監査モジュールの入出力契約である。

```text
evaluate_detector(human_labels, predictions)
  -> confusion_counts, precision, recall_decided,
     false_positive_rate_decided, decision_coverage,
     positive_coverage, detection_yield

evaluate_probability_forecasts(human_labels, probabilities)
  -> brier_score, prevalence, calibration_bins

compare_paired_cases(baseline_records, candidate_records, metric)
  -> per_case_differences, mean_difference, paired_count,
     missing_cases, uncertainty_summary

evaluate_stop_detector(completed_records, human_labels, detections)
  -> false_stop_rate, detection_rate, denominators
```

すべての関数は、未定義値とその理由、使用した母数を返す。課題識別子の重複、入力件数の不一致、ラベル範囲外、非有限の確率値は黙って補正せず、入力エラーとして扱う。

関数の検証には、全陽性・全陰性・全保留・空入力・欠損した対照・同一課題の複数生成を含める。特に「判定を全部保留すると高性能になる」「分母ゼロが満点になる」という誤った集計を防ぐ必要がある。

## 9. 参照先と調査範囲

- [ARA の原提案：Heretic PR #211](https://github.com/p-e-w/heretic/pull/211)
- [一般的な生成退化の研究：Holtzman ほか](https://arxiv.org/abs/1904.09751)
- [ローカル：モデル・ARA・生成処理](src/heretic/model.py)
- [ローカル：評価処理](src/heretic/evaluator.py)
- [ローカル：試行と評価の接続](src/heretic/main.py)
- [ローカル：最近傍距離計算](src/heretic/utils.py)

ローカルリンクは閲覧時の作業ツリーを開く。再現に必要な対象コミットは冒頭に記した。実装変更、モデルの実行、ブランチへの取り込みは本レポートの作成には含まれない。
